"""
Type definitions — tất cả định nghĩa kiểu dữ liệu của ứng dụng.

    types/http/      — Pydantic schemas cho HTTP boundary (giao tiếp với Client)
    types/internal/  — TypedDict cho internal layers (Repository → Service → Workflow)
"""
